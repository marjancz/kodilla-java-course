package com.kodilla.rps;

public class Scissor { //implements Figure {
    private Figure winner;

    public boolean checkGames(Figure player, Figure comp) {
       // return player.winner.equals(comp);
        return true;
    }
}
